Chat application with rooms using a C# Client Windows Client to connect to C++ Server. The C++ server implements threads, syncronization, semaphores, and calls. The C# client is built on Visual Studio and uses WinForms and TCP connections to communicate with the server effectively.

This was a group project for the Operating Systems Class 3yr @ Western.